# Use postgres/example user/password credentials

services:

  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: users_form
    ports:
      - "5402:5432"
    volumes:
      - db_data:/var/lib/postgresql/data

volumes:
  db_data:
